import { SocietyMemberInfoModel } from './society-member-info-model';

describe('SocietyMemberInfoModel', () => {
  it('should create an instance', () => {
    expect(new SocietyMemberInfoModel()).toBeTruthy();
  });
});
